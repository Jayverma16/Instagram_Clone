<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/transparent" /> <!-- Background color (transparent in this example) -->
    <stroke
        android:width="1dp"
        android:color="@color/grey"/> <!-- Border color -->
<corners android:radius="4dp" /> <!-- Border corner radius (adjust as needed) -->
</shape>